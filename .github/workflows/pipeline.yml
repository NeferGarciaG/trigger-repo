name: Pipeline on Argo Deploy

on:
  repository_dispatch:
    types: [argo_deploy]

jobs:
  # Job 1: registra el servicio que acaba de deployar como healthy
  set-status:
    runs-on: ubuntu-latest
    steps:
      - name: Set commit status for this service
        env:
          GH_TOKEN: ${{ secrets.GH_ARGO_TOKEN }}
          REVISION: ${{ github.event.client_payload.revision }}
          APP: ${{ github.event.client_payload.app }}
        run: |
          echo "📌 Registrando $APP como healthy @ $REVISION"
          gh api \
            --method POST \
            -H "Accept: application/vnd.github+json" \
            /repos/NeferGarciaG/argo-github/statuses/$REVISION \
            -f state=success \
            -f context="argocd/$APP" \
            -f description="Deployed healthy por ArgoCD"

  # Job 2: detecta qué servicios cambiaron en este commit y valida que todos estén healthy
  detect-and-check:
    needs: set-status
    runs-on: ubuntu-latest
    outputs:
      all_changed_healthy: ${{ steps.check.outputs.all_changed_healthy }}
      changed_services: ${{ steps.check.outputs.changed_services }}
      failed_services: ${{ steps.check.outputs.failed_services }}
    steps:
      - name: Detect changed services and check health
        id: check
        env:
          GH_TOKEN: ${{ secrets.GH_ARGO_TOKEN }}
          REVISION: ${{ github.event.client_payload.revision }}
          APP: ${{ github.event.client_payload.app }}
        run: |
          echo "🔍 Detectando servicios que cambiaron en $REVISION (disparado por $APP)"

          # Obtener archivos modificados en este commit
          CHANGED_FILES=$(gh api \
            -H "Accept: application/vnd.github+json" \
            /repos/NeferGarciaG/argo-github/commits/$REVISION \
            --jq '.files[].filename')

          echo "Archivos modificados:"
          echo "$CHANGED_FILES"

          # Mapear rutas de overlays a nombres de Application CRs
          # Patrón: svc-X/overlays/dev/... -> svc-X-dev
          CHANGED_SERVICES=$(echo "$CHANGED_FILES" \
            | grep -oP '^svc-[^/]+(?=/overlays/dev/)' \
            | sort -u \
            | sed 's/$/-dev/')

          if [ -z "$CHANGED_SERVICES" ]; then
            echo "ℹ️  Ningún overlay de servicio cambió en este commit — nada que validar"
            echo "all_changed_healthy=true" >> $GITHUB_OUTPUT
            echo "changed_services=" >> $GITHUB_OUTPUT
            echo "failed_services=" >> $GITHUB_OUTPUT
            exit 0
          fi

          echo ""
          echo "Servicios con cambios en este commit:"
          echo "$CHANGED_SERVICES"

          # Obtener commit statuses actuales para esta revisión
          STATUSES=$(gh api \
            -H "Accept: application/vnd.github+json" \
            /repos/NeferGarciaG/argo-github/commits/$REVISION/statuses)

          ALL_HEALTHY=true
          FAILED=()
          CHANGED_LIST=()

          while IFS= read -r SVC; do
            [ -z "$SVC" ] && continue
            CHANGED_LIST+=("$SVC")
            STATUS_COUNT=$(echo "$STATUSES" | jq \
              --arg ctx "argocd/$SVC" \
              '[.[] | select(.context == $ctx and .state == "success")] | length')

            if [ "$STATUS_COUNT" -gt 0 ]; then
              echo "  ✅ $SVC — healthy"
            else
              echo "  ❌ $SVC — NO reportó healthy (degradado o aún desplegando)"
              ALL_HEALTHY=false
              FAILED+=("$SVC")
            fi
          done <<< "$CHANGED_SERVICES"

          # Exportar resultados
          CHANGED_STR=$(IFS=','; echo "${CHANGED_LIST[*]}")
          FAILED_STR=$(IFS=','; echo "${FAILED[*]}")

          echo "changed_services=$CHANGED_STR" >> $GITHUB_OUTPUT
          echo "failed_services=$FAILED_STR"   >> $GITHUB_OUTPUT

          if [ "$ALL_HEALTHY" = "true" ]; then
            echo "all_changed_healthy=true" >> $GITHUB_OUTPUT
            echo ""
            echo "✅ Todos los servicios modificados están healthy"
          else
            echo "all_changed_healthy=false" >> $GITHUB_OUTPUT
            echo ""
            echo "❌ Servicios con fallo: ${FAILED[*]}"
            echo "⏳ Pipeline en espera — no todos los cambios están healthy"
          fi

  # Job 3: verifica si el commit contiene [release]
  check-release:
    needs: detect-and-check
    if: needs.detect-and-check.outputs.all_changed_healthy == 'true'
    runs-on: ubuntu-latest
    outputs:
      is_release: ${{ steps.check.outputs.is_release }}
    steps:
      - name: Check if revision is a release commit
        id: check
        env:
          GH_TOKEN: ${{ secrets.GH_ARGO_TOKEN }}
          REVISION: ${{ github.event.client_payload.revision }}
          SOURCE_REPO: NeferGarciaG/argo-github
        run: |
          COMMIT_MSG=$(gh api /repos/$SOURCE_REPO/commits/$REVISION --jq '.commit.message')
          echo "Revision : $REVISION"
          echo "Message  : $COMMIT_MSG"

          if echo "$COMMIT_MSG" | grep -qi "\[release\]"; then
            echo "is_release=true" >> $GITHUB_OUTPUT
            echo "✅ Release commit — pipeline correrá"
          else
            echo "is_release=false" >> $GITHUB_OUTPUT
            echo "⏭️  No es un release commit — skipping"
          fi

  # Job 4: ejecuta el pipeline real
  run-pipeline:
    needs: [detect-and-check, check-release]
    if: needs.check-release.outputs.is_release == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Run pipeline
        run: |
          echo "🚀 Ejecutando pipeline"
          echo "Servicios que cambiaron : ${{ needs.detect-and-check.outputs.changed_services }}"
          echo "App que disparó         : ${{ github.event.client_payload.app }}"
          echo "Env                     : ${{ github.event.client_payload.env }}"
          echo "Revision                : ${{ github.event.client_payload.revision }}"
          echo "Argo link               : ${{ github.event.client_payload.argo_link }}"
